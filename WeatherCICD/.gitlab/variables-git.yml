# ============================================================
# GitLab CI Deployment Variables Configuration
# ============================================================
# Define Git and report-specific variables for the pipeline
# These variables are used for managing Git operations and saving ZAP reports
# Make sure to adjust paths and credentials according to your project's needs

variables:
  GIT_USER_EMAIL:
    value: "gitlab-ci@ci.com"
    description: "Git author email (used for commits made by CI)."
  
  GIT_USER_NAME:
    value: "GitLab CI"
    description: "Git author name (used for commits made by CI)."
